# Issue #64 — Create a workflow to allow a user to save / read his preferences to a dedicated repository as database

**Issue number:** #64
**User request:** Tackle GitHub issue #64 with only specs and ordered sub-tasks
